[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Serve the Rebar app at the root URL (keeps the URL clean — no /rebar.html)
[[redirects]]
  from = "/"
  to = "/rebar.html"
  status = 200
